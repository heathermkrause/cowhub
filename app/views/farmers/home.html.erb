<h3>Welcome to Cow Hub</h3>
<h2> The live data repository for CARE Strengthening the Dairy Value Chain</h2>

<table>
  <tr>
    <th>Name</th>
    <th>Group</th>
    <th>Sex</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @farmers.each do |farmer| %>
  <tr>
    <td><%= farmer.name %></td>
    <td><%= farmer.group_id %></td>
    <td><%= farmer.sex %></td>
    <td><%= link_to 'Show', farmer %></td>
    <td><%= link_to 'Edit', edit_farmer_path(farmer) %></td>
    <td><%= link_to 'Destroy', farmer, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Farmer', new_farmer_path %>
